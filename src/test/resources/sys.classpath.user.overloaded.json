{
  "ncube": "sys.classpath",
  "axes": [
    {
      "name": "env",
      "type": "DISCRETE",
      "valueType": "STRING",
      "preferredOrder": 1,
      "hasDefault": true,
      "columns": []
    }
  ],
  "cells": [
    {
      "id": [],
      "type": "exp",
      "value": "string s = SystemUtilities.getExternalVariable('NCUBE_BOOTSTRAP');  if (SrinngUtilities.hasContent(s)) {
      def map = [:];
      if (s) {
        map = new JsonSlurper().parseText(s);
    }
    if (map.classpath) {
      return [ + map.classpath + ];
    }

    return ['http://www.cedarsoftware.com/tests/ncube/cp1']"
    }